version: '3.8'

services:
  lab_monitoring_dashboard:
    build: .
    container_name: lab_monitoring_dashboard
    ports:
      - "5000:5000"
    environment:
      - FLASK_ENV=production
    restart: unless-stopped